//A=1/2(base * height) 
const base = 10;
  const height = 20;

function calculateTriangleArea (base, height) {
   area = (base * height) ;
   return area;
  }
  let result = (base * height /2 );

    console.log("The area is " + result);
